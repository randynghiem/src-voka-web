import React from 'react';

/**
 * TODO: Pending implementation
 * @param {*} props default React properties
 */
const SearchPager = (props) => {
  return (
    <div>Search Pager</div>
  )
};

export default SearchPager;